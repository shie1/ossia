import type { NextPage } from "next";
import { usePlayer } from "../components/player";

const Test: NextPage = () => {
    const player = usePlayer()
    return (<>
        
    </>)
}

export default Test